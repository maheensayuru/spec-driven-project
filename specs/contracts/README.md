# System Contracts

This directory contains formal, machine-readable contracts and schemas:
- OpenAPI / Swagger specifications (`*.yaml` / `*.json`)
- JSON Schema definitions (`*.schema.json`)
- Protocol Buffers (`*.proto`)
- Interface & type definitions

All contracts placed here are versioned and treated as immutable once frozen.
